import sys

params = sys.argv[1:]
#pairs = [(params[a],params[a+1]) for a in range(0,len(params),2)]

for i,p in enumerate(params):
    if p == "init":
        #create new model
        pass
    elif p=="load":
        
        
        pass
